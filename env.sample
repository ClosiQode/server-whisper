# Variables d'environnement pour le serveur Whisper
# Copiez ce fichier en tant que .env et modifiez les valeurs selon vos besoins

# Clé secrète pour la génération des tokens JWT (OBLIGATOIRE)
# Utilisez une chaîne aléatoire et complexe pour plus de sécurité
JWT_SECRET_KEY=changez_cette_cle_par_une_valeur_securisee

# Identifiants administrateur (OBLIGATOIRE)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changez_ce_mot_de_passe

# Configuration du modèle Whisper (OBLIGATOIRE)
# Valeurs possibles: tiny, base, small, medium, large-v1, large-v2, large-v3
MODEL_SIZE=tiny

# Configuration du périphérique de calcul (OPTIONNEL)
# Valeurs possibles: cpu, cuda
DEVICE=cpu

# Type de calcul (OPTIONNEL)
# Valeurs possibles: float16, int8
COMPUTE_TYPE=int8

# Configuration du serveur (OPTIONNEL)
# HOST=0.0.0.0
# PORT=5000
